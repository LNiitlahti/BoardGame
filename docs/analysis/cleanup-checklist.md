# Cleanup Checklist

> Every actionable item from `technical-debt.md`, `junk-report.md`, `duplicate-code.md`, `security-report.md`, `performance-report.md`, `refactoring-opportunities.md`, and `dependency-report.md`, tiered by risk per the audit's Phase 9 criteria. IDs are stable references used by `risk-assessment.md` and `master-refactor-roadmap.md`. "Difficulty" is engineering effort; "Risk" is chance of an unnoticed regression (this app has **zero automated tests**, so every non-SAFE item relies on manual smoke-testing across the affected pages).

Legend: 🟢 SAFE · 🔵 LOW · 🟡 MEDIUM · 🔴 HIGH

> **Execution status**: this file is the full-detail reference and does not track in-progress/done state. `quick-wins.md` is the authoritative status board for the low-effort/high-impact subset of items below (checkboxes there are kept live). When an item from either file is actually completed, tick it off in **both** places — check `quick-wins.md` first before starting any item to avoid duplicate work.

---

## 🟢 SAFE — no behavior change, delete/rename/doc-only

- [ ] **CL-01** Delete `firestore.rules.temp`. *Difficulty: trivial. Risk: none — file is not referenced by any deploy tooling in this repo. Impact: removes a standing "loaded gun" — a permissive ruleset sitting under a deploy-able-sounding name. Dependencies: none. Benefit: closes the single highest-standing-risk item in security-report.md that requires zero code changes.* (security-report.md §3.1)
- [x] **CL-02** Delete the 5 `full/scripts/*_deprecated.js` files (`action-history_deprecated.js`, `spell-manager_deprecated.js`, `spells-god_deprecated.js`, `tournament-manager_deprecated.js`, `god-scripts_deprecated.js` — 5,892 lines). *Risk: none — zero live references confirmed by static grep and a dynamic-import sweep. Benefit: −5,892 lines of dead weight.* (junk-report.md (a))
- [x] **CL-03** Delete `full/admin_old.html` and, as a follow-on check, `full/scripts/admin-phase-adapter.js` if it becomes orphaned (it is currently `admin_old.html`'s only consumer). *Risk: none — zero references, zero unique functionality vs. current `admin.html`.* (junk-report.md (b), refactoring-opportunities.md §3.4)
- [x] **CL-04** Delete `BoardGame/lightweight_legacy/` in its entirety (17 files) — confirmed unreachable from every current entry point. *Risk: none if CL-25 (navbar dead-branch removal) is done in the same pass so nothing references the directory name afterward.* (junk-report.md (c), duplicate-code.md §1-3)
- [ ] **CL-05** Delete `full/css/styles.css` (38 KB, zero references anywhere). (junk-report.md (d))
- [ ] **CL-06** Delete the 8 orphaned CSS rule blocks in `full/css/ceremony.css` (`.ceremony-admin-panel`, `.ceremony-admin-step`, `.ceremony-admin-progress`, `.ceremony-points-value`, `.ceremony-points-delta[.negative]`, `.ceremony-hex-info`, `.ceremony-progress-fill`, `.ceremony-progress-text`). (junk-report.md (d))
- [ ] **CL-07** Delete the 8 orphaned CSS rule blocks in `full/css/phase-indicator.css` (`.phase-indicator-content/-left/-label/-icon/-name/-round/-center/-right`) and the paired dead JS in `full/scripts/phase-manager.js:1326-1335` (`nameEl`/`iconEl`/`roundEl`, permanently null `getElementById` lookups). (junk-report.md (d))
- [x] **CL-08** Delete `shared/images/hexes/default_coords/` (91 files, 8.07 MB) + `default_mountain_heart_v3.png` + `default_side_heart_v3.png` (8.56 MB total) — zero code references, confirmed backup artifacts from a hex-art regeneration commit. (junk-report.md (e), performance-report.md §4)
- [ ] **CL-09** Delete `shared/images/game-logos/Counter-Strike/` and `.../StarCraft/` (superseded by `_2`/`_II` folders, zero references). (junk-report.md (e))
- [ ] **CL-10** Tick off `REPO_UPKEEP_INSTRUCTIONS.md` §6 item 2 (DOM listener accumulation) as fixed, and item 6 (rate limiting) as "fixed for onboarding, not yet extended to chat/ready-check/rename" rather than leaving both as open unchecked boxes. Correct item 4's "8+" count to the verified 11. (performance-report.md §6, duplicate-code.md §4, security-report.md §7)
- [ ] **CL-11** Fix the stale entries in `docs/architecture/god-modules.md`: remove the `stateManager_deprecated.js` row from the Deprecated Files table (§10 — file deleted in commit `1127f1c`), remove/update the "Disputed Matches" panel and `getDisputedMatches()` description (removed in commit `960768a`), and update "Phase Flow (Current)" to reflect the consolidated `matches_in_progress` + slots model instead of the old 6-phase-per-match description. (junk-report.md cross-cutting note, refactoring-opportunities.md §6 side note)
- [ ] **CL-12** Fix the stale `lightweight/` path references throughout `REPO_UPKEEP_INSTRUCTIONS.md` and `docs/architecture/index.md`'s System Overview diagram (rename to `lightweight_legacy/`, or remove entirely once CL-04 lands and the directory no longer exists). Add the current `full/` pages missing from `index.md`'s diagram (`home.html`, `team.html`, `profile.html`, `match-queue.html`, `replay.html`, `view-onboarding.html`, `onboarding-status.html`). (project-overview.md)
- [x] **CL-13** Fix the two doc/rule mismatches in `docs/architecture/firestore-rules.md`: add `avatarUrl` to the owner-writable field list (§2), and correct the Permission Matrix's "User (owner)" column to show create/update rights, not read-only. (security-report.md §3.2)
- [ ] **CL-14** Correct the stale path entries in root `.gitignore` (`BoardGame/scripts/firebase.js` → `BoardGame/shared/scripts/firebase.js`; `BoardGame/dev-tools.html` → `BoardGame/dev/dev-tools.html`). Harmless today only because the nested `BoardGame/.gitignore` covers the real paths — fix so a future contributor working from the root file alone isn't misled. (project-overview.md)
- [ ] **CL-15** Add a one-line README/architecture-doc note that `BoardGame/package.json` (puppeteer) is intentionally untracked/gitignored and requires a local `npm install` — a fresh clone has no npm dependencies today with no explanation on record. (dependency-report.md §1)
- [x] **CL-16** Run `npm audit fix` in `BoardGame/` to clear the 4 known transitive vulnerabilities in puppeteer's dependency tree (`basic-ftp`, `ip-address`, `js-yaml`, `ws` — all dev-tooling-only, never shipped to the browser). (dependency-report.md §1)
- [x] **CL-17** Delete the broken `<script src="../shared/scripts/firebase.js">` tag at `full/replay.html:180` — it throws a `SyntaxError` on every page load (classic script loading ES-module source) and is provably dead weight since `firebase-loader.js` already gets everything it needs via a separate fetch-as-text path. (dependency-report.md §3c)

## 🔵 LOW RISK — small, well-scoped, easily reversible, low blast radius

- [ ] **CL-18** Escape `user.displayName` at `full/scripts/user-management.js:138` and `:501` using the `escapeHtml()` helper already defined in the same file (`:159-163`). *Difficulty: trivial (2 call sites, helper already exists and is proven correct elsewhere in the same file). Risk: near-zero — pure output-escaping, no behavior change for legitimate names. Impact: closes the top-ranked finding in security-report.md (lowest attacker privilege, highest victim privilege).* (security-report.md §6.1 top finding)
- [x] **CL-19** Point `god.html`'s Chart.js `<script>` tag at the local `shared/vendor/chart.min.js` instead of the unpinned `cdn.jsdelivr.net/npm/chart.js`, and remove the two unused CDN tags for jsPDF/AutoTable plus the unused `pdf-generator.js` include from `god.html`. *Difficulty: trivial (3-4 line change). Risk: low — Chart.js call site (`stats-manager.js:779`) is unaffected by which file provides the global; jsPDF/AutoTable/pdf-generator.js are confirmed unused on this page by repo-wide grep for their only caller. Impact: fixes version drift, removes ~400 KB dead weight, removes an internet-connectivity dependency from the primary tournament-director console at LAN events with no WAN uplink.* (dependency-report.md §4, performance-report.md §7)
- [x] **CL-20** Add `defer` to every local `<script src>` tag across all 7 major pages (`god.html`, `admin.html`, `home.html`, `view.html`, `team.html`, `statistics.html`, `replay.html`). *Risk: low — scripts are already order-dependent by document order only (no code relies on synchronous blocking specifically), confirmed by reading the construction sequence in `god-app.js`. Recommend doing one page at a time and smoke-testing, since there is no test suite to catch a load-order mistake automatically.* (performance-report.md §5)
- [ ] **CL-21** Delete `shared/images/game-logos/` orphans is covered by CL-09; separately, fix the `beerdrinking` config entry in `games-config.js:151` — either add the missing `Beer_Drinking/Beer_Drinking_icon.png` asset or remove the config entry so it can't 404 if ever selected. (junk-report.md (e))
- [x] **CL-22** Delete `BoardGame/cors.json` (its Storage-bucket CORS policy governs a bucket the app's client code never actually uses — no Storage SDK loaded anywhere) — or, if Storage is planned for revival, scope `origin` down from `["*"]` to the app's real hostname(s) now rather than later. *Recommend deletion given no current use; cheap to re-add correctly-scoped when/if Storage is wired in.* (security-report.md §5)
- [ ] **CL-23** Escape `team.name`/`team.color` once, at the point they're read out of `gameData.teams`/`gameState.teams`, rather than patching each of the ~10 render call sites individually (`admin.js`, `result-manager.js`, `admin-improved-adapter.js`, `admin-phase-adapter.js`, `team-controls.js`, `statistics.js`, `stats-manager.js`). *Difficulty: low-moderate (one normalization point, but needs a manual smoke-test pass across every listed file since there's no test suite). Risk: low — pure output-escaping. Impact: closes a stored-XSS chain that reaches the admin's own privileged session via a rules trade-off that's otherwise intentional and hard to close at the rules layer.* (security-report.md §6.2)
- [ ] **CL-24** Add a short client-side debounce/cooldown to `chat-module.js:_sendMessage` (same pattern as `onboarding.js`'s existing 500ms debounce) to stop accidental double-sends and trivial flooding. (security-report.md §7)
- [x] **CL-25** Remove the dead `getLightweightBasePath()` function and `item.lightweight` routing branch from `shared/scripts/navbar.js` (no `NAV_ITEMS` entry ever sets the flag; do this alongside CL-04 so nothing references the deleted directory afterward). (junk-report.md (c))

## 🟡 MEDIUM RISK — larger change, multiple files touched, needs manual regression pass

- [ ] **CL-26** Extract `GAMES_CONFIG.resolveDisplayName(gameState, gameId)` in `shared/scripts/games-config.js` and replace all 11 independent game-name-fallback implementations with it, fixing the 3 that currently reverse the documented priority order (`display-manager.js`, `onboarding.js`, `lightweight_legacy/view.html` — the latter moot once CL-04 lands, so really 2 live fixes). *Difficulty: moderate — small pure function, but 11 call sites across `admin.js`, `statistics.js`, `team-controls.js` (×2), `display-manager.js`, `stats-manager.js`, `team-manager.js`, `onboarding.js` each need a careful one-line swap and a manual smoke-test per page. Risk: moderate only because of the fan-out, not the logic itself — no test suite to catch a mis-swapped call site. Impact: highest-value duplication fix identified in the whole audit — it fixes an actual, currently-live behavioral inconsistency, not just a maintainability nitpick.* (duplicate-code.md §7b — the report's own top recommendation)
- [ ] **CL-27** Extract `classifyWinRate(rate)` (+ a class→color map) to a shared helper and replace all 11 occurrences across `statistics.js` (9) and `stats-manager.js` (2), including collapsing the 2 back-to-back class+color re-derivations. (duplicate-code.md §4)
- [ ] **CL-28** Extract `parseCoord(str) -> {q, r}` and replace the 13 hand-copied instances of the `/q(-?\d+)r(-?\d+)/` regex across `admin.js` (6), `board-manager.js` (2), `admin-improved-adapter.js`, `spell-engine.js`, `stats-manager.js` (3). (refactoring-opportunities.md §4.1)
- [ ] **CL-29** Expose a `renderTeamOwnership(boardData)` method on `shared/scripts/board-renderer.js` and replace the 3 independent hand-painting implementations in `admin.js:1474-1497`, `board-manager.js:56-74`, `team-controls.js:966-995` — fixes the already-live 5-vs-6-team-color cap inconsistency as a side effect. (refactoring-opportunities.md §5)
- [ ] **CL-30** Extract a shared `toggleModal(id)` helper and replace the 70 occurrences of inline `classList.add/remove('active')` across 10 files. *Lower priority than CL-26/28/29 — purely stylistic, no behavioral inconsistency has resulted from this one (yet).* (refactoring-opportunities.md §4.4, §7)
- [ ] **CL-31** Extract `ensureChatModule(tournamentId, teamId)` and replace the 3 copies of the chat-module bootstrap boilerplate in `admin.js:336-341`, `god-app.js:483-488`, `team-controls.js:126-128`. (refactoring-opportunities.md §7)
- [ ] **CL-32** Port `view.html`'s field-signature snapshot-gating pattern (`getViewRelevantSignature`) into `god-app.js` and `team-controls.js`'s snapshot handlers, and additionally gate board re-render specifically on `gameState.board`/`gameState.rooms` changes across all four live pages (`god.html`, `admin.html`, `team.html`, `view.html`). *Difficulty: moderate (~15 lines per page, pattern already proven in `view.html`). Risk: moderate — touches the core update path of every live page; needs a full manual pass through match creation, result confirmation, ready-check, and board placement on each page to confirm nothing silently stops updating when it should. Impact: the only performance item that plausibly causes noticeable stutter during real event usage (concurrent write bursts).* (performance-report.md §1-2, summary rec. #1)
- [ ] **CL-33** Add server-side authentication to `avatars/upload.php` — verify a Firebase ID token via the Admin SDK's `verifyIdToken()` and derive `uid` from the verified token instead of trusting the raw POST field. *Requires adding the Firebase Admin SDK (PHP has no official one — likely a REST call to Google's tokeninfo endpoint or a JWT library) to what is currently a dependency-free PHP script — genuinely new integration work, not a one-line fix, hence Medium rather than Low despite the endpoint being small.* (security-report.md §4)
- [ ] **CL-34** Add `.hasOnly([...])` field allowlists to the `chatTournament` and `chatTeams` create rules in `firestore.rules`, matching the pattern already used by the `users` and `tournaments` rules in the same file. *Requires editing the untracked `firestore.rules` file directly (see CL-35/risk-assessment.md for the process risk this implies) and testing against the Firestore emulator or a scratch project before deploying to production rules, since there is no automated rules test suite.* (security-report.md §3.2 item 3)
- [ ] **CL-35** Re-export `shared/images/hexes/coords/*.png` closer to their actual max on-screen size (~130-150px instead of 200px) — likely halves 6.9 MB to ~3.5 MB with no visible quality loss. *Medium because it's asset-regeneration work (needs the original art source or a careful downscale pass) and touches every hex image referenced across 4+ files, not a code change per se.* (performance-report.md §4)

## 🔴 HIGH RISK — cross-cutting, large blast radius, should not be done opportunistically

- [ ] **CL-36** Bring `firestore.rules` under real version control (a private repo, a protected branch, or at minimum a signed/dated changelog process) instead of the current local-file-only, gitignored arrangement. *High risk not because the rules content is dangerous to touch, but because changing *process* around the single authorization boundary for the whole app needs buy-in and a working deploy pipeline (none exists today — no `firebase.json`/`.firebaserc` in the repo) before it can be done safely. Doing this without first establishing how rules get deployed risks a bigger accidental gap than the one being fixed.* (security-report.md §3.0, architecture.md §5)
- [ ] **CL-37** Decompose `full/scripts/admin.js` (5,137 lines, 149 functions, ~18 responsibility clusters) into manager classes mirroring the `god.html` stack's existing 13-class decomposition — or, alternatively, retire `admin.html` in favor of routing `admin`-role users to a permission-scoped view of `god.html`. *Extremely high blast radius — this file is the entire admin-role user experience. No test suite exists to validate a decomposition didn't change behavior. Needs its own dedicated project with a written plan, not a line item.* (refactoring-opportunities.md §1, §8)
- [ ] **CL-38** Consolidate the 3 separately hand-maintained phase-order arrays (`phase-manager.js`'s `PHASE_ORDER`, `admin-improved-adapter.js`'s `ADMIN_PHASE_ORDER`, `admin-phase-adapter.js`'s `ADMIN_PHASE_ORDER`) into one shared source of truth, and update `display-manager.js`, `replay-engine.js`, `stats-manager.js`, and `god.html`'s hardcoded `<option>` list to import it. *High risk because the three arrays are already inconsistent with each other in production today (§6 of refactoring-opportunities.md) — reconciling them requires deciding which one is "correct" for each divergent phase, which is a product decision, not just a mechanical extraction.* (refactoring-opportunities.md §6)
- [ ] **CL-39** Rewire `UndoManager` to call into `ResultManager`/`BoardManager`'s own mutation methods instead of duplicating their logic directly on `gameState`. *High risk — undo is the one feature where a subtle behavioral change is hardest to notice until an admin needs it mid-event and it does the wrong thing. Requires careful, tested-by-hand verification of every undoable action type.* (refactoring-opportunities.md §3.1)
- [ ] **CL-40** Migrate off the Firebase JS SDK's deprecated "compat" API (v9.22.0 namespaced) to the modular API. *High risk / high effort — touches `window.firebaseXxx` call sites across dozens of files in `full/`, `shared/`, and (if not yet deleted) `lightweight_legacy/`. Not urgent (no CVE, just an API-shape deprecation notice from Firebase), so sequence last.* (dependency-report.md §3c)
- [ ] **CL-41** Upgrade vendored jsPDF (2.5.1 → 4.2.1) and jsPDF-AutoTable (3.8.4 → 5.0.8) together — version-coupled, requires re-testing every `doc.autoTable()` call site (7 of them in `pdf-generator.js`) and the overall PDF layout. *Not urgent — no exploitable CVE in this app's browser-only usage — but real work once undertaken.* (dependency-report.md §2)
- [ ] **CL-42** Stand up a first slice of automated test coverage (see `master-refactor-roadmap.md` Milestone 8) — not a single item so much as a prerequisite that makes every MEDIUM/HIGH item above meaningfully lower-risk once it exists.

---

**Totals**: 17 SAFE, 8 LOW, 10 MEDIUM, 7 HIGH — 42 actionable items. See `risk-assessment.md` for the reasoning behind each tier and rollback strategy per category, and `master-refactor-roadmap.md` for suggested sequencing into independently-mergeable milestones.
